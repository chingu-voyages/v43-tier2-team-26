{
  "name": "whenwhere",
  "version": "1.0.0",
  "description": "",
  "main": "./server/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd server && nodemon index.js",
    "client": "cd whenwhere && npm start",
    "build": "cd whenwhere && npm install && npm run build",
    "dev": "concurrently  \"npm run start\" \"npm run build\"",
    "startdev": "concurrently  \"npm run start\" \"npm run client\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.0.1",
    "nodemon": "^2.0.22"
  }
}
